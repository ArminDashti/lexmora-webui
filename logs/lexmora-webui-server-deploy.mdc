# Lexmora webui server deploy script (ssh t3 -p 80)

**Timestamp:** 2026-07-26 17:41 +03:30

## Task

Make `.armin/docker-scripts/run-on-docker-server.ps1` compatible with the T3 Docker server using `ssh t3 -p 80`, and keep the UI reachable at `https://lexmora.xaigrok.ir`.

## Commands executed

- `ssh -p 80 t3` — inventory (HAProxy, `lexmora-webui`, `t3-net`)
- Edited `.armin/docker-scripts/run-on-docker-server.ps1` / `.yaml`
- Split host publish into `docker-compose.publish.yml` (server uses expose-only)
- `.\.armin\docker-scripts\run-on-docker-server.ps1` (failed once on Windows short TEMP path; fixed; redeployed OK)
- Verified `https://lexmora.xaigrok.ir` → HTTP 200
- Verified `POST /api/v1/auth/login` with `Origin: https://lexmora.xaigrok.ir` → HTTP 200

## Important outputs

- HAProxy: `lexmora.xaigrok.ir` → `lexmora-webui:80` on `t3-net`
- Container after deploy: `lexmora-webui` Up, ports `80/tcp` (no host bind)
- Public URL: HTTPS 200; login via same origin proxy OK

## Result

Deploy script works over `ssh t3 -p 80`. Stack is live and reachable at https://lexmora.xaigrok.ir.

## Warnings

- First deploy attempt tore down the old container, then failed on `Remove-Item` with 8.3 TEMP path (`AC508~1.DAS`). Cleanup now uses `[IO.Path]::GetTempPath()` + `[IO.File]::Delete`.
- Do not log JWT tokens from login smoke tests.
