# Deploy lexmora-webui behind HAProxy (lexmora.xaigrok.ir)

- **Timestamp:** 2026-07-24
- **Host:** t3-new (`2.144.27.74`) via `ssh t3` (SSH MCP `irancell` timed out on `2.144.13.55`)

## Commands / actions

1. Inspected host: Docker, `t3-net`, HAProxy mounts/config/certs
2. Confirmed HAProxy already routes `lexmora.xaigrok.ir` → `lexmora-webui:80` (SNI + Host); PEM present
3. Filled `.armin/docker-scripts/run-on-docker-server.yaml` (`ssh: "ssh t3"`, `volume_dir: /home/cloud-admin/docker/lexmora-webui`)
4. Fixed server deploy script so `delete_image=yes` runs before local image upload/load
5. Ran `.\.armin\docker-scripts\run-on-docker-server.ps1` (local build → upload → compose up)

## Important outputs

- Container `lexmora-webui` Up on `t3-net` (also published host `:8082` via compose default)
- `docker exec haproxy wget http://lexmora-webui:80/` → HTML OK
- `curl --resolve lexmora.xaigrok.ir:443:127.0.0.1 https://lexmora.xaigrok.ir/` → **HTTP 200**, cert `CN=lexmora.xaigrok.ir`
- No `lexmora-api` / `lexmora-pgsql` containers running

## Result

Web UI is live at **https://lexmora.xaigrok.ir**. HAProxy config did not need changes. API/login will fail until `lexmora-api` is deployed on `t3-net`.

## Warnings

- SSH MCP server entry `irancell` points at a different IP and timed out; used shell SSH alias `t3`
- Flat YAML parser rejects inline comments on values (removed from `build_image_on`)
